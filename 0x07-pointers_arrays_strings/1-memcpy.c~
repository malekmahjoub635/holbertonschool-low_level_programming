#include "holberton.h"
/**
 *memcpy - a function that copies memory area
 * Return: 0
 */
char *_memcpy(char *dest, char *src, unsigned int n)
{
int j;
for (j = 0; j < n && src[dest] != '\0'; j++)
{
dest[j] = src[j];
}
while (j < n)
{
dest[j] = '\0';
j++;
}
return (dest);
}
